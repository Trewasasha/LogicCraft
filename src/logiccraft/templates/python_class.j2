"""Generated from UML Diagram: {{ diagram_name | trim }}"""
from typing import List, Optional, Dict, Any

{% for node in nodes %}
    class {{ node.name | trim }}:
    """Class generated from UML diagram"""
    {% if node.properties %}

        # Attributes
        {% for prop in node.properties %}
            _{{ prop.name | trim }}: {{ prop.type | trim }}
        {% endfor %}
    {% endif %}

    def __init__(self):
    {% if node.properties %}
        {% for prop in node.properties %}
            self._{{ prop.name | trim }} = None
        {% endfor %}
    {% else %}
        pass
    {% endif %}
    {% if node.methods %}

        # Methods
        {% for method in node.methods %}
            {% set rtype = (method.return_type | trim) if method.return_type else "None" %}
            def {{ method.name | trim }}(self{% if method.parameters %}, {{ generate_method_params(method) }}{% endif %}) -> {{ rtype }}:
            pass
        {% endfor %}
    {% endif %}

{% endfor %}